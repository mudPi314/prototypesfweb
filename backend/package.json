{
  "name": "deface-backend",
  "version": "1.0.0",
  "type": "module",
  "description": "Backend server for the Deface Program",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "prod": "echo 'Your IP address is:' && (command -v ipconfig >/dev/null && ipconfig getifaddr en0 || hostname -I) && NODE_ENV=production node server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}